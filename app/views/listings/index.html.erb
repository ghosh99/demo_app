<div class="container-narrow">

<h2>View all listings</h2>

<hr>

<table>
  <tr>
    <th>Headline</th>
    <th>Description</th>
    <th>Location</th>
    <th>Picture<th> 
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @listings.each do |listing| %>
  <tr>
    <td><%= listing.headline %></td>
    <td><%= listing.description %></td>
    <td><%= listing.location %></td>
    <td><%= image_tag listing.image1_url(:thumb).to_s, alt: "Picture" %></td>

    <td><%= listing.user.name %></td>
    <td><%= link_to 'View', listing %>|</td>
    <td><%= link_to 'Edit', edit_listing_path(listing) %>|</td>
    <td><%= link_to 'Delete', listing, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<hr>
<div class="btn btn-medium">
  <%= link_to 'New Listing', new_listing_path %>
</div>

</div>